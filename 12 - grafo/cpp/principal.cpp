#include <iostream>
#include <cstdlib>

using namespace std;

#include "grafo.h"

int main() {
    
    Grafo *grafo = NULL;

    grafo = inicializar(5);
    // grafo = inserirAresta(0, 1, grafo);
    // grafo = inserirAresta(0, 2, grafo);
    // grafo = inserirAresta(1, 3, grafo);
    // grafo = inserirAresta(2, 3, grafo);
    // grafo = inserirAresta(3, 1, grafo);
    // grafo = inserirAresta(3, 4, grafo);
    // grafo = inserirAresta(4, 2, grafo);

    // exibir(grafo);

    return 1;
}